# Repository Operating Rules

이 문서는 `emfls.github.io` 저장소에서 작업하는 모든 Codex 세션의 기본 지침이다.

## 작업 시작 순서

1. `AGENTS.md`를 확인한다.
2. `PROJECT_HISTORY.md`의 최근 기록만 확인한다.
3. `TASKS.md`에서 현재 작업 또는 최고 우선순위 미완료 작업 1개를 선택한다.
4. 현재 작업과 직접 관련된 파일만 조사한다.
5. 최소 범위로 수정한다.
6. 관련 테스트를 먼저 실행하고 위험할 때만 전체 테스트를 실행한다.
7. 핵심 결과를 `PROJECT_HISTORY.md`에 간결하게 기록한다.
8. `TASKS.md`의 상태와 후속 작업을 갱신한다.

이미 이력에 기록된 구조와 원인을 매번 처음부터 재조사하지 않는다. 사용자가 별도 범위를 지정하지 않으면 한 번에 하나의 우선순위 작업만 완료한다.

## Token / Context Efficiency

- 현재 작업과 직접 관련된 파일만 읽고 전체 저장소 탐색을 반복하지 않는다.
- `PROJECT_HISTORY.md`에 기록된 구조·원인·결정을 다시 분석하지 않는다.
- 같은 검색 명령과 분석을 불필요하게 반복하지 않는다.
- 전체 빌드는 공통 생성 로직, 배포, 광범위한 회귀 위험이 있을 때만 수행한다.
- 가장 작은 관련 테스트부터 실행한다.
- 장황한 분석보다 실제 수정과 검증을 우선한다.
- 작업 범위 밖 리팩터링과 미확인 파일 정리를 하지 않는다.
- 사용자 소유의 미추적·수정 파일을 보존한다.
- 최종 답변은 핵심 결과, 테스트, 다음 작업만 가능하면 10줄 안팎으로 보고한다.
- 이미 완료한 내용을 다시 장황하게 설명하지 않는다.

## 구현 원칙

- 임시 하드코딩 대신 근본 원인을 수정한다.
- 자동 생성 문제는 생성 결과물 하나가 아니라 생성·정규화·검증 로직을 수정한다.
- 기존 기능, URL, canonical, GA4, AdSense, 검색엔진 인증을 깨뜨리지 않는다.
- 모바일 호환성과 키보드 접근성을 유지한다.
- 데이터가 의심스러우면 잘못된 값보다 `N/A`, 상태 표시 또는 숨김을 우선한다.
- 검색 수요 없는 대량 페이지, doorway, 지역명 치환 페이지를 만들지 않는다.
- PAGE_SCORE보다 실제 검색 의도, 사용자 가치, 수익 데이터를 우선한다.
- WINNER와 COOLDOWN 페이지의 보호 규칙을 지킨다.
- AdSense CTR을 최적화하거나 클릭을 유도하지 않는다.
- 명백한 공통 오류가 아니면 작업 범위를 확장하지 않는다.

## 주요 경로

- 홈페이지·정적 사이트: `index.html`, `kor/`, `eng/`, `jpn/`
- 미국·한국 주식 앱 소스: `kor/stockwiki/src/`
- 주식 원본 JSON: `kor/stockwiki/data/stocks/`
- 주식 빌드 결과: `kor/stockwiki/stocks/`
- 무료 도구: `util/`
- 브라우저 게임: `game/`
- 감사·생성·수익 자동화: `scripts/`
- GitHub Actions: `.github/workflows/seo-qa.yml`
- 성과·상태 데이터: `data/`
- 회귀 테스트: `tests/`

`kor/stockwiki/stocks/` 같은 생성 결과를 직접 고치기 전에 반드시 원본 JSON과 `kor/stockwiki/src/` 및 실제 데이터 생성 경로를 확인한다.

## 완료 기준

완료 선언 전 다음을 모두 수행한다.

- 수정 파일과 의도치 않은 변경 확인
- 최소 관련 테스트 통과
- 주요 출력 또는 렌더링 결과 확인
- `PROJECT_HISTORY.md` 기록
- `TASKS.md` 상태 반영

배포가 포함되면 GitHub Pages와 SEO QA 성공까지 확인한다.
