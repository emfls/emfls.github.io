# GA4 도구 완료 이벤트 운영 검증

- 검증일: 2026-08-14
- 운영 URL: `https://emfls.github.io/kor/util/camping-packing-checklist/`
- 대상 GA4 속성: `exchat-b9ce8` (속성 ID `226808916`)

## 검증 결과

1. 운영 페이지가 정상 로드되고 캠핑 조건별 목록 생성 기능이 동작했다.
2. 광고 영역과 상호작용하지 않고 `목록 만들기` 버튼만 실행했다.
3. GA4 실시간 개요에서 해당 페이지 조회 1건을 확인했다.
4. 실시간 이벤트 목록에서 `tool_complete` 1건을 확인했다.
5. `tool_complete`는 주요 이벤트로 집계되지 않아 기존 운영 원칙을 유지했다.

## 판정

운영 배포본의 도구 완료 이벤트가 GA4 속성까지 정상 전송된다. 맞춤 측정기준의 일반 보고서 반영은 소급되지 않으므로 등록 이후 데이터가 쌓이는 기간을 기다려야 한다.

## 후속 확인 시점

- 48시간 이후: `tool_name`, `result_type` 값이 일반 보고서와 탐색에서 사용 가능한지 확인
- 7일 이후: 도구별 `tool_complete / page_view` 비율 비교
- 28일 이후: 완료율과 검색 유입이 높은 도구로 내부 링크 및 후속 콘텐츠 우선 배정
